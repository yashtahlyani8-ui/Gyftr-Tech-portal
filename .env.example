# Leave these blank to run in local demo mode (data persists in the browser).
# Fill them in to connect a real Supabase project (schema in supabase/schema.sql).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
